--------------------------------------------------
-- Create Database
--------------------------------------------------

CREATE DATABASE CompanyDB;

USE CompanyDB;

--------------------------------------------------
-- Create Employee Table
--------------------------------------------------

CREATE TABLE Employee(
    emp_id INT PRIMARY KEY,
    employee_name VARCHAR(50) NOT NULL,
    street VARCHAR(100) NOT NULL,
    city VARCHAR(50) NOT NULL
);

--------------------------------------------------
-- Create Company Table
--------------------------------------------------

CREATE TABLE Company(
    company_name VARCHAR(50) PRIMARY KEY,
    city VARCHAR(50) NOT NULL
);

--------------------------------------------------
-- Create Works Table
--------------------------------------------------

CREATE TABLE Works(
    employee_name VARCHAR(50),
    company_name VARCHAR(50),
    salary DECIMAL(10,2) NOT NULL,

    PRIMARY KEY(employee_name, company_name),

    FOREIGN KEY(company_name)
    REFERENCES Company(company_name)
);

--------------------------------------------------
-- Create Manages Table
--------------------------------------------------

CREATE TABLE Manages(
    employee_name VARCHAR(50),
    manager_name VARCHAR(50),

    PRIMARY KEY(employee_name),

    FOREIGN KEY(employee_name)
    REFERENCES Employee(employee_name)
);

--------------------------------------------------
-- Insert Records into Employee
--------------------------------------------------

INSERT INTO Employee VALUES
(101,'Aditya','JM Road','Pune'),
(102,'Rahul','FC Road','Mumbai'),
(103,'Sneha','MG Road','Delhi'),
(104,'Pooja','Camp','Hyderabad');

--------------------------------------------------
-- Insert Records into Company
--------------------------------------------------

INSERT INTO Company VALUES
('TCS','Mumbai'),
('InfoSys','Pune'),
('TechM','Hyderabad');

--------------------------------------------------
-- Insert Records into Works
--------------------------------------------------

INSERT INTO Works VALUES
('Aditya','TCS',25000),
('Rahul','InfoSys',18000),
('Sneha','TechM',12000),
('Pooja','TechM',9000);

--------------------------------------------------
-- Insert Records into Manages
--------------------------------------------------

INSERT INTO Manages VALUES
('Aditya','Rahul'),
('Rahul','Sneha'),
('Sneha','Pooja');

--------------------------------------------------
-- 1. Find names of all employees
-- who work for 'TCS'
--------------------------------------------------

SELECT employee_name
FROM Works
WHERE company_name='TCS';

--------------------------------------------------
-- 2. Find employee names and company names
-- sorted by company ascending and employee
-- descending
--------------------------------------------------

SELECT employee_name,
       company_name
FROM Works
ORDER BY company_name ASC,
         employee_name DESC;

--------------------------------------------------
-- 3. Change city of employees working
-- with InfoSys to 'Bangalore'
--------------------------------------------------

UPDATE Employee
SET city='Bangalore'
WHERE employee_name IN
(
    SELECT employee_name
    FROM Works
    WHERE company_name='InfoSys'
);

--------------------------------------------------
-- 4. Find names, street address and city
-- of employees working for 'TechM'
-- and earning more than 10000
--------------------------------------------------

SELECT Employee.employee_name,
       Employee.street,
       Employee.city
FROM Employee
JOIN Works
ON Employee.employee_name = Works.employee_name
WHERE Works.company_name='TechM'
AND Works.salary > 10000;

--------------------------------------------------
-- 5. Add Column Asset to Company table
--------------------------------------------------

ALTER TABLE Company
ADD Asset DECIMAL(12,2);